services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "127.0.0.1:5433:5432"
    environment:
      POSTGRES_DB: clouddeck
      POSTGRES_USER: clouddeck
      POSTGRES_PASSWORD: clouddeck_dev
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U clouddeck"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
