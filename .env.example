# Database
DB_PASSWORD=changeme_strong_password_here
DATABASE_URL=postgresql://clouddeck:changeme_strong_password_here@localhost:5432/clouddeck

# Auth
NEXTAUTH_SECRET=generate-with-openssl-rand-base64-32
NEXTAUTH_URL=http://localhost:3000

# Encryption (for API key storage) - generate with: openssl rand -hex 32
ENCRYPTION_SECRET=generate-with-openssl-rand-hex-32

# App
PORT=3000
NODE_ENV=development
